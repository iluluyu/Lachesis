## 任务
- 我想要完成一个operator的集合，集合内有M个元素, 这些pauli算符有以下3个属性，第一个是pauliint，储存着N个整数，每个整数有4个选择“0，1，2，3”，然后就是pauli-string，储存N个字符，字符也是只有4个选择“I,X，Y, Z”，最后是一个数weight，默认为1，我需要访问这个集合中的每个元素的属性，请帮助我完成这些问题的解决方案，使用python语言
## 数据的读取
- 需求：我需要实现一个张量网络基础，我想要实现的是根据mode来进行张量数据的选择，根据不同的mode返回不同的矩阵。根据这些门来设置相关不同的数据，但是实际我的门参数是一个int数据，我需要对这些数据进行转换。下面是我写的相关代码，有没有可以优化的方面？
- 数据类型 我的参数配置中分为三类，一类是Pauli算符 门算符 和 测量算符。pauli算符，在`mode = 1`时分为4维度，在`mode = 2` 时只有两维度，只区分I和非I 的X, Y, Z.门算符和测量算符是根据不同的作用下给出对应算符的矩阵。